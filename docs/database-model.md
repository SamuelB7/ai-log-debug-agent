# Modelo de dados

O modelo relacional sera implementado no servico `backend-api` via Prisma.

Neste bootstrap, `services/backend-api/prisma/schema.prisma` contem apenas datasource e generator. Modelos serao adicionados junto com as regras de negocio.

